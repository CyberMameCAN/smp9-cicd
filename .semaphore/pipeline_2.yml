version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Build
    task:
      jobs:
        - name: Build
          commands:
            - checkout
            - echo "$DOCKER_PASSWORD" | docker login  --username "$DOCKER_USERNAME" --password-stdin
            - 'docker pull $DOCKER_USERNAME/smp9-cicd-production:1.17'
            - 'docker build -f Dockerfile.production --cache-from $DOCKER_USERNAME/smp9-cicd-production:latest -t $DOCKER_USERNAME/smp9-cicd-production:latest .'
            - 'docker push $DOCKER_USERNAME/smp9-cicd-production:latest'
      secrets:
        - name: dockerhub
